{
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "postdocs:build": "echo issambackyet.com > docs/CNAME",
    "docs:preview": "vitepress preview",
    "buildanddeploy": "npm run docs:build && git add . && git commit -m 'deploy' && git push origin main -f"
  },
  "dependencies": {
    "vitepress": "^1.0.0-rc.29"
  }
}
